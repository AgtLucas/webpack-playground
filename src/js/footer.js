const React = require('react');

require('scss/modules/footer');

const Footer = React.createClass({
  render () {
    return (
      <footer divClass="main-footer">
        Footer
      </footer>
    );
  }
});

module.exports = Footer;
